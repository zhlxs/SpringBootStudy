feign:
  url:
    xxl-job-svc: "http://localhost:8089/xxl-job-admin"

### 调度中心部署跟地址 [选填]：如调度中心集群部署存在多个地址则用逗号分隔。执行器将会使用该地址进行"执行器心跳注册"和"任务结果回调"；为空则关闭自动注册；
xxl:
  job:
    admin:
      addresses: http://localhost:8089/xxl-job-admin
    executor:
      appname: boot-job
      ip:
      port: 8000
      logpath:
      logretentiondays: -1
    accessToken: